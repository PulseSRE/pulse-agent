{{/*
Secrets should be created externally and referenced via existingSecret.
This template is intentionally empty to prevent inline credential storage.
Use: kubectl create secret generic gcp-sa-key --from-file=key.json=./sa-key.json
*/}}
